# Check the type of variable assigned using input () function.

a = input("Enter something: ") # enter any data type value

print("The type of variable assigned is:", type(a)) # print the type of variable

# by default the input() function returns the type as string